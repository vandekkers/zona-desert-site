// SITE V2 — carried over verbatim from the platform-era page (shelved at
// app/_platform/site/terms). Content unchanged by design; re-exported so the
// text has exactly one source of truth.

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Terms & Conditions | Zona Desert",
  robots: { index: true, follow: true }
};

export { default } from "@/app/_platform/site/terms/page";
