// SITE V2 — carried over verbatim from the platform-era page (shelved at
// app/_platform/site/cookie-policy). Content unchanged by design; re-exported so the
// text has exactly one source of truth.

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cookie Policy | Zona Desert",
  description: "How Zona Desert Property Solutions uses cookies and similar technologies on this site.",
  robots: { index: true, follow: true }
};

export { default } from "@/app/_platform/site/cookie-policy/page";
